# このリポジトリについて

`index.html`（単一ファイル、約9,000行）と`style.css`（約820行）で構成される、ホステスクラブの
フロア管理Webアプリ。Firebase Realtime Databaseでリアルタイム同期している。

役割は2種類：
- `内勤`（admin）：管理側。卓一覧の編集、初回タブの編集、集計、パスワード管理などができる。
- `キャスト`（host）：現場スタッフ側。予定報告タブがメインの操作画面。卓一覧・初回・全体タブは
  基本的に閲覧のみ（編集は内勤専用）。

## 保留中のタスク：コードの全体バグ検証（未着手）

2026年9月26日、ユーザーから「プログラマー視点で一回隅々までバグ検証してほしい」という依頼が
あり、作業量が大きいためブロックごとに分けて進める方針にした。**まだどのブロックも着手していない。**

次にこの話が出たら、以下の表をそのまま使って「どのブロックからやるか」を聞き直せばよい
（キャスト側の操作がスムーズであることを内勤側より優先したいという方針が明言されている）。

| # | ブロック | 主な内容 | 想定時間 | キャストの関与度 |
|---|---|---|---|---|
| 1 | 基盤・Firebase同期層 | 初期化処理、全onValueリスナー、fb保存関数群、State管理（`<script type="module">`部分、約950行） | 45〜60分 | 裏側（間接的に最重要）。ログイン状態・個別パスワード・スタッフ一覧などキャストが使う全機能の土台。過去に見つけた誤ログアウトバグもここ起因 |
| 2 | ログイン・パスワード・端末認証 | ログイン画面、個別パスワード、本人確認（あなたは誰ですか）、ログアウト、呼び名編集（約1450行） | 45〜60分 | ★高（毎回操作） |
| 3 | 予定報告タブ | キュー入力、アイバン、備考ポップアップ、本日分リセット、報告状況表示（約900行） | 40〜50分 | ★★最高（キャストのメイン画面） |
| 4 | 卓一覧タブ | セル編集、表描画、退店時間自動計算、長押し編集、卓状況（約1060行） | 45〜60分 | 中（キャストは閲覧のみ。編集は`canEditList`でadmin限定） |
| 5 | 付け回し機能 | 幹部のヘルプ卓管理まわり全体（約1800行、単独最大） | 60〜90分 | 中（「内勤・幹部のみ利用可」制限あり。一般キャストは関与なし、幹部キャストは操作する） |
| 6 | 初回タブ | 初回タブ本体、ミニピッカー、全体タブへの同期（約510行） | 30分 | 低（キャストは閲覧のみ、編集はadmin限定） |
| 7 | 全体タブ・場内・特記事項 | 全体ビュー描画、場内メモ、特記事項（約380行） | 20〜25分 | 中（キャストもよく見る。編集操作は少ない） |
| 8 | 集計・統計・お客様履歴 | 集計ロジック、統計表示、お客様履歴検索、リセット復元（約700行） | 35〜45分 | ほぼ無し（内勤専用） |
| 9 | 一覧管理モーダル・共通UI部品 | 一覧管理、固定ヘッダー、トースト、スワイプ、モーダル共通処理（約880行） | 40〜50分 | 一部あり（トースト/スワイプ/モーダル共通処理はキャストも触れるが、一覧管理自体は内勤専用） |
| 10 | HTML構造・CSS | 画面の骨組み（body側）とstyle.css全体（約1030行） | 25〜35分 | 全体に影響（キャスト画面の見た目にも直結） |

合計目安：おおよそ7〜8時間分。

キャスト目線の優先度順（触れる頻度が高い順）：**3→2→1→7→4→6→9→10→5→8**

## 開発ルール（このリポジトリでの作業で毎回やること）

- 開発ブランチ：`claude/modal-time-color-change-4xava9`。作業後は必ず`main`にもcherry-pickして
  push する（本番は`main`を参照している）。
- コミット前に必ずNode構文チェック（`<script>`本体を抽出して`new Function()`に通す）を実行する。
- ロジックが絡む修正は、可能な限りNode上で簡易シミュレーションして動作を確認してから push する。
- `main`へcherry-pick後は`git diff origin/claude/modal-time-color-change-4xava9 main -- index.html`
  が空であることを確認してからpushする。
